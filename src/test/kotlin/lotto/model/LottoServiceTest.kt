package lotto.model

import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test

class LottoServiceTest {
    @Test
    fun `랜덤 6개 번호가 중복되지 않는다`() {
        // given
        // when
        // then
        assertThrows<IllegalArgumentException> {
        }
    }
}